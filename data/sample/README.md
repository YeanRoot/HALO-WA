# Sample Data

This directory is reserved for small examples or smoke-test inputs.

The full HALO-WA checkpoints and demo buffers are distributed through the
`artifacts-v1` GitHub Release and are downloaded into `data/artifacts/` by
`scripts/download_artifacts.sh`.
